{
  "name": "insieme",
  "version": "0.0.3",
  "description": "An offline-first collaborative library with authoritative server for real-time data synchronization",
  "keywords": [
    "offline-first",
    "collaborative",
    "real-time",
    "synchronization",
    "authoritative-server",
    "javascript"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuusoft-org/insieme.git"
  },
  "homepage": "https://github.com/yuusoft-org/insieme#readme",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": {
      "import": "./src/index.js"
    }
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "oxlint": "^1.26.0",
    "puty": "^0.0.5-rc1",
    "vitest": "^4.0.8"
  },
  "scripts": {
    "lint": "oxlint && bunx prettier --check src",
    "lint:fix": "bunx prettier --check --write src",
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage",
    "types": "bunx tsc"
  },
  "lint-staged": {
    "src/**/*.{js}": [
      "oxlint && bunx prettier --write"
    ]
  }
}
